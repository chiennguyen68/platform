export enum LanguageEnum {
    vi = 'vi',
    en = 'en',
}

export enum PostStatusEnum {
    draft = 'draft',
    published = 'published',
}

export enum FormValidationEnum {
    required = 'required',
    maxLength = 'maxLength',
}
